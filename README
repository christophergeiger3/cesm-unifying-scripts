
        To run:
        Change the `cesm_root` variable in the config.sh to the root of your cesm directory.
        Make `config.sh` executable with `chmod +x ./config.sh`.
        Run `./config.sh && make`.



        The Community Earth System Model version 1.2.0 (CESM1.2.0)

                      http://www.cesm.ucar.edu

        See the CESM web site for documentation and information.
